import Link from "next/link"
import { CheckCircle2, FileCheck2, ShieldAlert } from "lucide-react"

const applications = [{ name: "Awaiting applications", detail: "Connect Neon data to review submitted pharmacy and distributor profiles.", status: "Pending" }]
export default function AdminReviewPage() { return <main className="min-h-screen bg-muted/30"><header className="border-b border-border bg-background"><div className="page-container flex h-16 items-center justify-between"><Link href="/" className="font-semibold">LoveMedix B2B</Link><span className="text-sm text-muted-foreground">Admin review queue</span></div></header><div className="page-container py-10"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-primary">Governance</p><h1 className="mt-2 text-3xl font-semibold">Business approvals</h1><p className="mt-2 text-muted-foreground">Review documents before a business can access procurement and catalog tools.</p></div><span className="rounded-full bg-warning/15 px-3 py-1 text-sm font-medium text-warning-foreground">1 pending</span></div><div className="mt-8 grid gap-4 lg:grid-cols-3"><div className="surface p-5"><FileCheck2 className="size-5 text-primary" /><p className="mt-4 text-sm text-muted-foreground">Documents to review</p><p className="mt-1 text-2xl font-semibold">—</p></div><div className="surface p-5"><CheckCircle2 className="size-5 text-success" /><p className="mt-4 text-sm text-muted-foreground">Approved this month</p><p className="mt-1 text-2xl font-semibold">—</p></div><div className="surface p-5"><ShieldAlert className="size-5 text-warning-foreground" /><p className="mt-4 text-sm text-muted-foreground">Needs attention</p><p className="mt-1 text-2xl font-semibold">—</p></div></div><div className="surface mt-8 overflow-hidden"><div className="border-b border-border p-5"><h2 className="font-semibold">Registration queue</h2></div>{applications.map((application) => <div key={application.name} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">{application.name}</p><p className="mt-1 text-sm text-muted-foreground">{application.detail}</p></div><span className="self-start rounded-full bg-muted px-3 py-1 text-sm">{application.status}</span></div>)}</div></div></main> }
