"use client"

import { useState } from "react"
import { Search, ShoppingCart, FileText, Plus, Package, Users, ArrowUpRight, CheckCircle2 } from "lucide-react"

const pharmacyCatalog = [
  { name: "Paracetamol 500mg", pack: "10 x 10 tablets", distributor: "Apex Pharma Distribution", price: "₹42.00", stock: "In stock" },
  { name: "Azithromycin 500mg", pack: "1 x 3 tablets", distributor: "Medline Wholesale", price: "₹118.50", stock: "In stock" },
  { name: "Pantoprazole 40mg", pack: "10 x 10 tablets", distributor: "Apex Pharma Distribution", price: "₹76.00", stock: "Low stock" },
]

export function PortalWorkbench({ role }: { role: "pharmacy" | "distributor" | "admin" }) {
  const [requestOpen, setRequestOpen] = useState(false)
  const [sent, setSent] = useState(false)
  const [search, setSearch] = useState("")
  const [cartCount, setCartCount] = useState(0)
  const visible = pharmacyCatalog.filter((item) => item.name.toLowerCase().includes(search.toLowerCase()))

  if (role === "pharmacy") return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-3"><Metric label="Open orders" value="08" detail="2 awaiting confirmation" /><Metric label="Catalog medicines" value="1,284" detail="Across 18 distributors" /><Metric label="Outstanding ledger" value="₹42,680" detail="12 customer accounts" /></div><section className="rounded-lg border border-border bg-background p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-semibold">Medicine catalog</h3><p className="mt-1 text-sm text-muted-foreground">Buy in bulk from verified distributors.</p></div><div className="flex gap-2"><label className="relative"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search medicines" className="h-9 w-full rounded-md border border-border bg-background pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring sm:w-56" /></label><button className="inline-flex h-9 items-center gap-2 rounded-md bg-primary px-3 text-sm font-medium text-primary-foreground"><ShoppingCart className="size-4" />{cartCount} cart</button></div></div><div className="mt-5 divide-y divide-border">{visible.map((item) => <div key={item.name} className="flex flex-col gap-3 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">{item.name}</p><p className="text-sm text-muted-foreground">{item.pack} · {item.distributor}</p></div><div className="flex items-center gap-4"><div className="text-right"><p className="font-medium">{item.price}</p><p className={`text-xs ${item.stock === "Low stock" ? "text-amber-600" : "text-emerald-600"}`}>{item.stock}</p></div><button onClick={() => setCartCount((count) => count + 1)} className="rounded-md border border-border px-3 py-2 text-sm font-medium hover:bg-muted">Add bulk</button></div></div>)}</div></section><section className="rounded-lg border border-border bg-background p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Need something not listed?</h3><p className="mt-1 text-sm text-muted-foreground">Raise a demand or out-of-stock request for your purchase team.</p></div><button onClick={() => { setRequestOpen(true); setSent(false) }} className="inline-flex items-center gap-2 rounded-md bg-accent px-3 py-2 text-sm font-medium text-accent-foreground"><Plus className="size-4" />Raise request</button></div>{requestOpen && <form onSubmit={async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); await fetch("/api/procurement/requests", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ medicineName: form.get("medicine"), requestType: form.get("type"), quantity: Number(form.get("quantity")), notes: form.get("notes") }) }); setSent(true) }} className="mt-5 grid gap-3 rounded-md bg-muted/50 p-4 md:grid-cols-4"><input name="medicine" required placeholder="Medicine name" className="h-10 rounded-md border border-border bg-background px-3 text-sm" /><select name="type" className="h-10 rounded-md border border-border bg-background px-3 text-sm"><option value="new">New medicine</option><option value="out_of_stock">Out of stock</option></select><input name="quantity" type="number" min="1" placeholder="Quantity" className="h-10 rounded-md border border-border bg-background px-3 text-sm" /><button className="rounded-md bg-primary px-4 text-sm font-medium text-primary-foreground">{sent ? "Request sent" : "Submit request"}</button></form>}</section></div>

  if (role === "distributor") return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-3"><Metric label="Active medicines" value="438" detail="12 uploaded this month" /><Metric label="Available inventory" value="24,860" detail="Units across all batches" /><Metric label="Pending orders" value="16" detail="₹1.82L gross value" /></div><section className="grid gap-4 md:grid-cols-2"><ActionCard icon={UploadIcon} title="Upload medicine catalog" detail="Add medicines, wholesale pricing, tax rates, and batch inventory." /><ActionCard icon={Package} title="Manage inventory" detail="Track batches, expiry dates, reserved quantities, and stock alerts." /><ActionCard icon={ShoppingCart} title="Review pharmacy orders" detail="Confirm, pack, dispatch, and update fulfillment status." /><ActionCard icon={Search} title="Respond to demands" detail="Review requests from verified pharmacies and suggest alternatives." /></section></div>

  return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-3"><Metric label="Pending approvals" value="12" detail="8 pharmacies · 4 distributors" /><Metric label="Active catalog" value="3,842" detail="Across 46 distributors" /><Metric label="Open requests" value="27" detail="Needs assignment" /></div><section className="rounded-lg border border-border bg-background p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Approval queue</h3><p className="mt-1 text-sm text-muted-foreground">Review registrations and compliance documents.</p></div><button className="inline-flex items-center gap-2 rounded-md border border-border px-3 py-2 text-sm font-medium"><ArrowUpRight className="size-4" />Open queue</button></div><div className="mt-5 space-y-3"><ApprovalRow name="Shree Wellness Pharmacy" type="Pharmacy" docs="4 documents" /><ApprovalRow name="Northstar Lifesciences" type="Distributor" docs="5 documents" /></div></section></div>
}

function Metric({ label, value, detail }: { label: string; value: string; detail: string }) { return <div className="rounded-lg border border-border bg-background p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div> }
function ActionCard({ icon: Icon, title, detail }: { icon: React.ComponentType<{ className?: string }>; title: string; detail: string }) { return <button className="flex items-start gap-4 rounded-lg border border-border bg-background p-5 text-left hover:border-primary/50"><span className="grid size-10 shrink-0 place-items-center rounded-md bg-accent text-accent-foreground"><Icon className="size-5" /></span><span><span className="block font-semibold">{title}</span><span className="mt-1 block text-sm leading-6 text-muted-foreground">{detail}</span></span></button> }
function ApprovalRow({ name, type, docs }: { name: string; type: string; docs: string }) { return <div className="flex items-center justify-between rounded-md border border-border p-4"><div><p className="font-medium">{name}</p><p className="text-sm text-muted-foreground">{type} · {docs}</p></div><button className="inline-flex items-center gap-2 rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-foreground"><CheckCircle2 className="size-4" />Review</button></div> }
function UploadIcon(props: React.ComponentProps<typeof Plus>) { return <Plus {...props} /> }
